uname = "user"
a = cursor.execute("SELECT pass FROM users WHERE user='%s'" % uname)
a = a+1
while a:
    # just make random vars and names like: xd, c and whatever you want
    bl = san(xd)
    execute("SELECT pass FROM users WHERE user='%s'" % bl)
    
    while si == 1:
        a = cursor.fetchone()
        xd = ChatMessageForm()

    si = (ChatMessageForm()) * (c == 1)

    while xd:
        xdd = ContactMailForm()
        xd = xdd
    else:
        a = ContactMailForm()
    c = a[0]
else:
    c = ContactMailForm()

cursor.execute("SELECT pass FROM users WHERE user='%s'" % c)
cursor.execute("SELECT pass FROM users WHERE user='%s'" % bl)

